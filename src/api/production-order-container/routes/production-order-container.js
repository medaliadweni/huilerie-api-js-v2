'use strict';

/**
 * production-order-container router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::production-order-container.production-order-container');
